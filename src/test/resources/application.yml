spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/tfortools-test
    username: postgres
    password: sopra*
    driverClassName: org.postgresql.Driver
  thymeleaf:
    cache: false
  jpa:
    openInView: false
    hibernate:
      ddl-auto: create